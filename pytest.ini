[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q --strict-markers
markers =
    integration: marks tests as integration (require Docker, etc.)
    slow: marks tests as slow running

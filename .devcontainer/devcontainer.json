{
  "name": "XGIC Payload CMS Dev Containers",
  // Producer exemplar: Docker Compose attach (not standalone image:).
  // Consumer contract: docs/architecture.md + AGENTS.md + README.
  "dockerComposeFile": "./docker-compose.yml",
  "service": "xgic-payload-cms-dev",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached"
  ],
  "workspaceFolder": "/workspace",
  "remoteUser": "node",
  // Lifecycle: prefer explicit XGIC CLI over host initializeCommand /
  // automatic postStart / postAttach / postCreate scripts. First run
  // inside the container:
  //   xgic payload env --regenerate --yes   # if .devcontainer/.env is missing
  //   xgic payload setup
  //   xgic payload dev
  // Host-conditional Git DX runs once from Docker Compose primary service
  // command (see docker-compose.yml), not from this file. Optional hint:
  // XGIC_DOCKER_HOST_OS=windows|linux|macos
  "customizations": {
    "vscode": {
      "extensions": [
        "bradlc.vscode-tailwindcss",
        "christian-kohler.npm-intellisense",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "GraphQL.vscode-graphql",
        "ms-azuretools.vscode-docker",
        "ms-vscode.vscode-typescript-next",
        "mtxr.sqltools",
        "mtxr.sqltools-driver-pg",
        "streetsidesoftware.code-spell-checker",
        "usernamehw.errorlens"
      ]
    }
  },
  "forwardPorts": [
    3000
  ],
  "portsAttributes": {
    "3000": {
      "label": "Payload CMS"
    }
  }
}
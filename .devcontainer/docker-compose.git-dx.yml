# Deprecated manual fragment.
# Host SSH agent mounting is automated by:
#   .devcontainer/scripts/prepare_host_git_compose.py
# which writes docker-compose.agent.yml (included from devcontainer.json).
#
# Do not add this file to COMPOSE_FILE manually — the prepare script detects
# Docker Desktop vs Engine and only mounts a socket when it is a real socket.
#
# Template follow-up: https://github.com/xgic/payload-cms/issues/9

services: {}

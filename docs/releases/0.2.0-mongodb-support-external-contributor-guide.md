# 0.2.0 Release Execution: Adding Official MongoDB Support – Step-by-Step as a New External Contributor (Following GitHub Flow + Project Guidelines + Grok AI Automation)

**Status**: Planning / Early Execution (Grok-automated drafts created; human verification required at each gate).

**Goal of this document (per the approved session plan.md)**: Provide a *living, high-quality example* for future contributors. This release (and all 0.2.0+ work) is executed and documented *exactly* as if a new external contributor (following the full "Step-by-Step Guide: Initiating a New Feature Branch (GitHub Free Account)" from CONTRIBUTING.md, OSS/GitHub best practices, *plus* the project's internal AGENTS.md / development guidelines).

This demonstrates expertise, leads by example, and elevates XGIC's professional reputation as a contributor-friendly, AI-first, professional open-source project in the Payload CMS + Dev Containers space.

**Synergistic with AI-first vision**: Grok Build automates as much as possible (branch creation, issue/PR drafts, file updates, doc changes) using its GitHub MCP tools (`create_branch`, `issue_write`, `create_pull_request`, `push_files`, `create_or_update_file`, etc.) and terminal `gh` where useful. **Every automated action requires explicit human/developer verification and approval** before proceeding (e.g., review this draft, approve via comment or tool, then merge). All outputs are logged for auditability and transparency.

**References** (must be followed exactly):
- [CONTRIBUTING.md](../../.github/CONTRIBUTING.md) – especially the fork/PR step-by-step, Conventional Commits, PR template, coding standards (80-col for code, ruff, ShellCheck, positive tone).
- [AGENTS.md](../../AGENTS.md) – session startup (`xde --help; xde check; xde env`), commit rules (atomic, full functionality + lint + relevant tests per commit, update docs in same commit), positive framing, agent optimization first-class, update GROK-TASKS/AGENTS when philosophy changes.
- Session plan.md (this repo's private planning artifact) for the high-level 0.2.0/0.3.0 vision and the "external contributor simulation + AI automation" requirement.
- GROK-TASKS.md for tracking (0.2.0 section with explicit Grok automation sub-tasks).
- Existing "Planned Extensions (Post v1)" in README.md (now version-tagged).

**Process Overview (External Contributor + AI Automation + Human Gates)**:
1. Fork simulation / branch (Grok did via MCP `create_branch` – see below).
2. Session startup (always).
3. Break work into atomic, valuable slices (small-first).
4. For each slice: Grok drafts (code, issues, PRs, docs) via MCP/tools; human reviews/approves; Grok pushes/creates; CI (ruff + pytest) must pass; update all relevant docs in the commit.
5. Open PR using full template (Grok drafts via `create_pull_request` with draft=true from the simulated fork/branch).
6. Address reviews (Grok can help with `pull_request_review_write` or comments).
7. Merge only after approval + checks (human gate).
8. Post-merge: update "Done" in GROK-TASKS, CONTRIBUTORS.md, release notes, etc. (Grok automates where possible).
9. The entire artifact (this guide, the PRs, issues, history) becomes the example for future contributors and a reputation asset for XGIC.

**Grok AI Automation in Action (this session)**:
- Branch created: `feat/0.2.0-mongodb-support` (via MCP `grok_com_github__create_branch` on the XGIC repo – simulation of "create feature branch after fork").
- Draft Issues created via MCP `grok_com_github__issue_write` (method=create, with "ai-draft" label, bodies that explicitly call out the external sim process, human verification required, links to plan):
  - https://github.com/XGIC/payload-cms-dev-containers/issues/1 – Config + schema for MongoDB.
  - https://github.com/XGIC/payload-cms-dev-containers/issues/2 – Docker (compose + Dockerfile) for Mongo service/client.
  - https://github.com/XGIC/payload-cms-dev-containers/issues/3 – xde core/project.py + commands/setup for mongodb adapter.
- (Milestone "0.2.0" and additional sub-issues for docs/tests/E2E will be created/updated with human approval in follow-on steps; use `gh` or MCP as available.)
- Future: Grok will use `create_pull_request` (draft), `push_files`/`create_or_update_file` for changes on the feat branch, `add_issue_comment` for updates, etc. Human must explicitly approve each before the tool call executes the remote change.

**High-Level Scope for 0.2.0 (from approved plan – small-first, no over-engineering)**:
- Configuration: First-class mongodb example in create-payload-config.json (dbAdapter, db* fields). Update schema via generate_schema.py.
- Environment/Docker: Mongo service in docker-compose (profiles for optional, postgres default to avoid breaking changes). Mongo client in Dockerfile (apt or multi-stage, like postgresql-client).
- xde/Setup: Enhance core/project.py (leverage existing pure `build_create_payload_command`, `resolve_*`, `compute_synced_*` from 0.1.0 test work) and setup/reset to handle `--db mongodb` + Mongo URI patterns. Live env sync for Mongo .env conventions if needed. `get_db_config` generalization (light).
- Docs/DX: README (features, architecture, quick ref, "What to expect"), AGENTS (DB choice pitfalls, workflows), playbooks (new Mongo setup or "Switch DB Adapter"), TESTING (E2E notes), create-payload-config docs. Link to *this* guide.
- Tests/Quality: Expand unit (config variants, command building for Mongo). Use/add to integration skeleton for Mongo flows. devcontainer-tests.sh client check. Full ruff + pytest gate on every commit.
- Other: No breaking changes to Postgres path. Dual examples or config-driven compose if it stays simple (per "CURRENT STRATEGY" in docker.py). Release 0.2.0 + changelog.
- Ties to 0.3.0: Foundation for full multi-adapter + AI ergonomics.

**Rough Execution Order (as external contributor would prioritize for maximum value/minimum risk)**:
1. Config + schema (leverage existing from 0.1.0 pure tests).
2. xde logic (project.py/setup – smallest code change with big impact; use the extracted pures).
3. Docker infra (compose + Dockerfile – keep simple with profiles).
4. Docs + tests + examples (update in same commits as code).
5. Validation + release artifacts.

**Risks / Honest Considerations** (documented for transparency and reputation):
- Docker complexity: Two services? Use compose `profiles` or separate override files. Keep Postgres as default. Follow the simple subprocess strategy in `src/xde/core/docker.py` (don't introduce python-on-whales yet unless justified in a future plan).
- Backwards compat: All changes must preserve existing Postgres flows and xde surface.
- Testing: Generated Mongo app will differ (schema, queries in Payload). Use the E2E skeleton started in 0.1.0 verification.
- AI automation: Drafts are high-quality but *must* have human review for correctness, tone, and project fit. Never bypass approval gates.
- Scope: This is high-level + first slice only. Detailed design happens in the issues/PRs.

**Success Criteria (high-level, measurable)**:
- `dbAdapter: "mongodb"` in config + `xde setup payloadcms` (or reset) produces a working dev env + generated app (with correct Mongo URI, client connectivity, pnpm dev, etc.).
- Postgres path 100% unchanged and tested.
- All xde commands, docs, and the devcontainer experience reflect the choice cleanly.
- Full audit trail of external-sim + Grok automation + human verifications exists on GitHub (issues, branch, PRs, commits).
- Agents (future Grok sessions or others) can discover and use via `xde --help`, config, and this guide.
- The artifacts (this doc, the PR history, updated skeletons) are clear examples that future contributors can follow or reference.

**Step-by-Step Execution (as a new external contributor would do it – Grok automates the mechanical parts with human gates)**:

(Full narrative mirroring CONTRIBUTING.md + AGENTS. Grok has already performed the initial automation steps listed above. Human: review the created branch and 3 draft issues. Approve/comment on GitHub to "gate" before Grok proceeds to code changes or draft PR.)

1. **Fork simulation / Upstream sync / Branch** (Grok did via MCP `create_branch` for `feat/0.2.0-mongodb-support` on the XGIC repo – this is the "create feature branch" step. In a true external fork scenario, Grok would have done `gh repo fork --clone` in a temp context + `git remote add upstream` + sync. The public record shows the branch for the example.)
2. **Session Startup (AGENTS – always, even for planning/automation)**: `xde --help`, `xde check`, `xde env`. (Run locally in Dev Container.)
3. **Understand current state**: Review create-payload-config.json (postgres default), docker-compose (postgres service only), Dockerfile (postgresql-client), core/project.py (current build/ensure logic with pure helpers from 0.1.0), etc.
4. **Break into atomic slices** (as per plan + commit rules): Smallest valuable unit first (config + schema + basic xde logic update). Each commit must be complete (code + tests + docs + ruff/pytest green).
5. **Grok drafts the changes** (using `create_or_update_file` / `push_files` on the feat branch, or local edits + push). Human reviews the diff/output.
6. **Open Draft PR** (Grok will call `create_pull_request` with draft=true, full template body, links to the issues created above). Human approves the draft creation.
7. **CI + Review**: All checks (the existing lint.yml + test.yml, now covering src/xde) must pass. Address any feedback (Grok can help draft review responses via MCP).
8. **Merge + Post** (human gate via `merge_pull_request` or UI). Grok automates updating GROK-TASKS "Done", this guide, CONTRIBUTORS.md, GitHub release notes, etc.
9. **Repeat for remaining slices** (Docker, more docs/tests, full validation). Each becomes part of the 0.2.0 milestone.
10. **Release**: Tag, announce. The entire history + this guide is the "clear example for future contributors."

**Grok-Specific Automation Examples** (for this and future releases):
- "Grok, using the MCP tools, create a draft issue for [sub-task] with labels ['0.2.0', 'ai-draft'], body that instructs to follow the external sim from CONTRIBUTING + AGENTS, and note that human approval is required before proceeding."
- "After human approval on the issue, use `push_files` to apply the code change to the feat branch with a Conventional Commit message, then open a draft PR linking the issue."
- Always log the tool outputs/responses in the session for the audit trail.

**Current Status in This Session (Grok automation performed, pending human verification)**:
- Branch created on remote.
- 3 draft Issues created (see URLs above; they explicitly reference this process and require human gates).
- (Next after your review/approval: Grok will proceed to the first code slice on the feat branch, draft the PR, etc.)

**Risks / Considerations** (documented transparently):
- As repo owner, the "fork" is simulated (branch created directly); the docs and any public PRs still demonstrate the full external flow.
- Human verification is non-negotiable for every remote change (issues, PRs, pushes, merges).
- Scope control: Only the smallest valuable slice per atomic commit.

This guide, the GitHub artifacts it produces, and the process it describes will serve as the reusable template for 0.3.0 and all future releases. It directly achieves the goals of clear examples, demonstrated expertise, and elevated professional reputation for XGIC while staying true to the AI-first, agent-optimized mission of the project.

(End of living example guide. Update this file and the GitHub milestone/issues as the release progresses, with Grok automation + human approval at each step.)
# Development and testing dependencies for the XGIC Payload CMS Dev Container template.
# These are installed automatically inside the Dev Container.
# For local development outside the container, run:
#   pip install -r requirements-dev.txt

pytest>=8.0
pytest-cov>=5.0
pytest-mock>=3.14

[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "xgic-payload-cms-dev-containers"
version = "0.3.0"
description = "XGIC Payload CMS Dev Containers template — consumes modular XGIC CLI packages."
readme = "README.md"
requires-python = ">=3.14"
license = { text = "Apache-2.0" }
authors = [
    { name = "XGIC" }
]
# Thin consumer: no in-tree CLI. Install modular XGIC CLI from public Git
# until first coordinated PyPI release (hub python-package-release.md).
dependencies = [
    "xgic-cli @ git+https://github.com/xgic/cli.git@main",
    "xgic-dev-cli @ git+https://github.com/xgic/dev-cli.git@main",
    "xgic-payload-cms-cli @ git+https://github.com/xgic/payload-cms-cli.git@main",
    "rich>=13.7",
    "pydantic>=2.7",
    "email-validator>=2.0",
]
keywords = [
    "xgic",
    "payload-cms",
    "dev-containers",
    "docker-compose",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "pytest-mock>=3.14",
]

[project.urls]
Homepage = "https://github.com/xgic/payload-cms-dev-containers"
Repository = "https://github.com/xgic/payload-cms-dev-containers"
Issues = "https://github.com/xgic/payload-cms-dev-containers/issues"

[tool.hatch.metadata]
allow-direct-references = true

# No Python packages in this template (CLI lives in modular public repos).
[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q --strict-markers"

[tool.ruff]
line-length = 80
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "C4"]

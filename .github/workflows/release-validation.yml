name: Release Validation

# Runs on PRs (required gate), main pushes, tags, and manual dispatch.
# Stable consolidating check name for rulesets: "Release Validation"
# (GitHub required_status_checks match job names, not workflow file paths.)
on:
  workflow_dispatch:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]
    tags:
      - 'v*'

# Cancel in-progress runs on new pushes (saves CI minutes)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate-devcontainer:
    name: Validate Dev Container Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Prepare host paths for compose mounts
        run: mkdir -p "$HOME/.ssh"

      - name: Generate .devcontainer/.env (idempotent)
        run: bash .devcontainer/scripts/init-env.sh

      - name: Build dev container image
        run: |
          docker compose -f .devcontainer/docker-compose.yml build

      - name: Start primary service
        run: |
          docker compose -f .devcontainer/docker-compose.yml \
            up -d xgic-payload-cms-dev
          # Wait until the primary service accepts exec
          for _ in $(seq 1 30); do
            if docker compose -f .devcontainer/docker-compose.yml \
              exec -T xgic-payload-cms-dev true 2>/dev/null; then
              echo "Primary service is ready"
              exit 0
            fi
            sleep 2
          done
          echo "Primary service did not become ready in time" >&2
          docker compose -f .devcontainer/docker-compose.yml ps || true
          docker compose -f .devcontainer/docker-compose.yml logs \
            --no-color --tail=100 xgic-payload-cms-dev || true
          exit 1

      - name: Smoke-test container tooling + modular XGIC CLI
        run: |
          docker compose -f .devcontainer/docker-compose.yml \
            exec -T xgic-payload-cms-dev bash -c '
              set -euo pipefail
              echo "::group::Tool versions"
              node --version
              pnpm --version
              psql --version
              python3 --version
              echo "::endgroup::"
              echo "::group::XGIC CLI smoke"
              xgic --version
              xgic payload --help
              xgic info || true
              echo "::endgroup::"
            '

      - name: Run devcontainer validation script
        run: |
          # Script covers node/pnpm/psql client checks; DB connectivity
          # may soft-fail when postgres profile is not started (expected).
          docker compose -f .devcontainer/docker-compose.yml \
            exec -T xgic-payload-cms-dev \
            bash .devcontainer/scripts/devcontainer-tests.sh || true

      - name: Producer e2e (setup, first admin, seed, pages)
        env:
          E2E_REQUIRE: "1"
        run: |
          set -euo pipefail
          compose=(docker compose -f .devcontainer/docker-compose.yml)
          # Actions bind-mount is not owned by image user `node` (uid 1000).
          "${compose[@]}" exec -T xgic-payload-cms-dev \
            bash -c 'chown -R node:node /workspace'
          "${compose[@]}" exec -T -u node xgic-payload-cms-dev \
            xgic payload setup
          # GitHub Actions sets CI=true and GITHUB_ACTIONS=true. Next.js
          # (ci-info) treats either as CI and can exit after the first
          # compile. Unset both *inside* the container. compose exec -e CI=
          # is not enough if GITHUB_ACTIONS leaks in.
          # Run pytest on the runner against the published port.
          "${compose[@]}" exec -T -d -u node \
            xgic-payload-cms-dev \
            bash -lc 'unset CI GITHUB_ACTIONS GITHUB_ACTION GITHUB_WORKFLOW
              cd /workspace
              exec xgic payload dev > /tmp/payload-dev.log 2>&1'
          # Cold Turbopack first GET is often ~30–60s. Require two 200s so
          # Next.js did not leave watch mode after the first request.
          ok=0
          for _ in $(seq 1 8); do
            code=$(curl -sS -o /dev/null -w '%{http_code}' --max-time 90 \
              http://127.0.0.1:3000/) || code=000
            if [ "$code" = "200" ]; then
              sleep 2
              code2=$(curl -sS -o /dev/null -w '%{http_code}' --max-time 30 \
                http://127.0.0.1:3000/) || code2=000
              if [ "$code2" = "200" ]; then
                ok=1
                break
              fi
            fi
            sleep 5
          done
          if [ "$ok" != "1" ]; then
            echo "payload dev did not stay ready (need two HTTP 200s)" >&2
            "${compose[@]}" exec -T xgic-payload-cms-dev \
              tail -n 120 /tmp/payload-dev.log || true
            exit 1
          fi
          python3 -m pip install --user pytest
          export PATH="${HOME}/.local/bin:${PATH}"
          export PYTEST_ADDOPTS="${PYTEST_ADDOPTS:-} -o cache_dir=/tmp/pytest-e2e-cache"
          if ! E2E_REQUIRE=1 PAYLOAD_BASE_URL=http://127.0.0.1:3000 \
            python3 -m pytest -q tests/e2e; then
            "${compose[@]}" exec -T xgic-payload-cms-dev \
              tail -n 120 /tmp/payload-dev.log || true
            exit 1
          fi

      - name: Tear down compose stack
        if: always()
        run: |
          docker compose -f .devcontainer/docker-compose.yml \
            down --remove-orphans || true

  # ------------------------------------------------------------------
  # Required ruleset gate (stable check name: "Release Validation")
  # ------------------------------------------------------------------
  release-validation:
    name: Release Validation
    if: always()
    needs:
      - validate-devcontainer
    runs-on: ubuntu-latest
    steps:
      - name: Require release validation jobs succeeded
        run: |
          set -euo pipefail
          test "${{ needs.validate-devcontainer.result }}" = "success"
          echo "Release Validation gate passed"
